name: CI/CD
run-name: ${{ inputs.description || 'CI/CD' }}

on:
  pull_request:
  push:
    branches: [
      develop,
    ]
  workflow_dispatch:
    inputs:
      description:
        required: true
        type: string
        description: Short job run description
